// lib/theme/colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // プライマリカラー
  static const primary = Color.fromRGBO(44, 193, 172, 1);
  static const primaryPressed = Color.fromRGBO(26, 143, 127, 1);
  
  // 背景色
  static const darkBackground = Color.fromARGB(255, 44, 44, 44);
  static const lightBackground = Colors.white;
}